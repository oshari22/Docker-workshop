name: Build & Deploy to test
on:
  push:

jobs:
  Deploy-to-test-cluster:
    runs-on: self-hosted
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v2
      - name: build
        run: |
            docker build . -t oshari/nginx:osh2
            docker push oshari/nginx:osh2
